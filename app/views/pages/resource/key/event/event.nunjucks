{% extends "../sharable.nunjucks" %}

{% block page %}
    <div>
        <article class="wrapper-horizontal-stripes">
            <div class="horizontal-stripe article-header white-background">
                <div class="container--normal">
                    <div class="row">
                        <header class="col-xs-12 text-center">
                            <nav class="article-header__category article-header__category--deep">
                                <span class="article-header__category__upper">
                                    <a href="{$ gb.locales.urlPrefix $}/resource/search?contentType=event">{$ __('enums.cms.contentType.event') $}</a>
                                </span>
                                <span class="article-header__category__lower">{$ main.fields.start.value | dateRange(main.fields.end.value, false, gb.locales.current) $}</span>
                            </nav>
                            <h1 class="rtl_{$ main.fields.title.isRtl $}">{$ main.fields.title.value $}</h1>

                            <div class="article-header__button">
                                {% if main.fields.primaryLink %}
                                <a href="{$ resolved.Entry[main.fields.primaryLink.sys.id].fields.url.value $}" class="gb-button--discreet small rtl_{$ resolved.Entry[main.fields.primaryLink.sys.id].fields.label.isRtl $}">{$ resolved.Entry[main.fields.primaryLink.sys.id].fields.label.value $}</a>
                                {% endif %}
                                <a ng-href="/api/newsroom/events/{$ main.sys.id $}.ics" class="gb-button--discreet small rtl-supported"><span class="gb-icon-calendar"></span> {$ __('resource.addToCalendar') $} </a>
                            </div>

                            <div class="article-header__highlights">
                                <div><span class="gb-icon-calendar"></span>
                                    {$ main.fields.start.value | dateRange(main.fields.end.value, false, gb.locales.current) $}
                                </div>
                                {% if main.fields.__singleDay and not main.fields.allDayEvent %}
                                <div>
                                    <span class="gb-icon-clock"></span>{$ main.fields.start.value | timeRange(main.fields.end.value, gb.locales.current) $}
                                    {% if main.fields.showTimeZone %}
                                    <span> UTC {$ main.fields.start.value | formatDate(gb.locales.current, 'Z') $}</span>
                                    {% endif %}
                                </div>
                                {% endif %}

                                {% if main.fields.country %}
                                <div><a href="#where" class="inherit rtl-supported"><span class="gb-icon-marker"></span>{$ __('country.' + main.fields.country.sys.id) $}</a></div>
                                {% endif %}

                            </div>

                        </header>
                    </div>
                </div>
            </div>

            {% include "../primaryImage.nunjucks" %}

            <div class="horizontal-stripe white-background">

                <div class="container--narrow">
                    {% include "../body.nunjucks" %}

                    <div class="article-secondary">
                        {% include "../documentsLinks.nunjucks" %}

                        <section class="rtl-supported">
                            <span class="relative-anchor-offset"><a id="where"></a></span>
                            {#<h3>Where</h3>#}
                            <dl class="inline">
                                {% if main.fields.venue %}
                                    <div>
                                        <dt>{$ __('resource.venue') $}</dt>
                                        <dd class="rtl_{$ main.fields.venue.isRtl $}">{$ main.fields.venue.value | safe $}</dd>
                                    </div>
                                {% endif %}
                                {% if main.fields.location %}
                                    <div>
                                        <dt>{$ __('resource.location') $}</dt>
                                        <dd class="rtl_{$ main.fields.location.isRtl $}">{$ main.fields.location.value | safe $}</dd>
                                    </div>
                                {% endif %}
                                {% if main.fields.country %}
                                    <div>
                                        <dt>{$ __('resource.country') $}</dt>
                                        <dd>{$ __('country.' + main.fields.country.sys.id) $}</dd>
                                    </div>
                                {% endif %}
                                <div>
                                    <dt>{$ __('resource.when') $}</dt>
                                    {% if main.fields.allDayEvent %}
                                        <dd>{$ main.fields.start.value | dateRange(main.fields.end.value, false, gb.locales.current) $}</dd>
                                    {% else  %}
                                        <dd>
                                            {$ main.fields.start.value | dateRange(main.fields.end.value, true, gb.locales.current) $}
                                            {% if main.fields.showTimeZone %}
                                                <span> UTC {$ main.fields.start.value | formatDate(gb.locales.current, 'Z') $}</span>
                                            {% endif %}
                                        </dd>
                                    {% endif %}
                                </div>
                                {% if main.fields.eventLanguage %}
                                    <div>
                                        <dt>{$ __('resource.language') $}</dt>
                                        <dd class="rtl_{$ main.fields.eventLanguage.isRtl $}">{$ main.fields.eventLanguage.value $}</dd>
                                    </div>
                                {% endif %}
                            </dl>
                        </section>
                    </div>

                    {% include "../auxiliary.nunjucks" %}
                </div>
            </div>

        </article>
    </div>
    {% include "../preview.nunjucks" %}
{% endblock %}
